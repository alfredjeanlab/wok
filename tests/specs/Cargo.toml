[package]
name = "specs"
version = "0.1.0"
edition = "2021"

[lib]
path = "lib.rs"

[dev-dependencies]
assert_cmd = "2.0"
predicates = "3.0"
regex = "1.0"
serde_json = "1.0"
similar-asserts = "1.7"
tempfile = "3.10"
yare = "3"

[target.'cfg(unix)'.dev-dependencies]
libc = "0.2"
